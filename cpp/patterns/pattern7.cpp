#include <iostream>
using namespace std;
int main()
    [int rows, coef = 1;
     cout << "Enter number of rows \n";
     cin >> rows;
     for (int i = 0; i < rows; i++)
     {
         for (int space = 1; space <= rows - i; space++)
         {
             cout << " ";
         }
         for (int j = 0;)
     }]